{{#each blogs as |blogs|}}



<div class="post">
    <div class="postin">
    <h1 class="header-post">{{blogs.name}}</h1>
    <h3 class="center">{{blogs.description}}</h3>
    </div>
</div>




{{/each}}